#!/bin/bash

# JupyterLab + Binder: Demonstrating how to get JupyterLab working with Binder 
# https://github.com/binder-examples/jupyterlab
# Widgets. Should already be enabled ?
#jupyter labextension install @jupyter-widgets/jupyterlab-manager
jupyter nbextension enable --py widgetsnbextension --sys-prefix
#jupyter nbextension enable --py bqplot --sys-prefix
jupyter nbextension enable --py ipyvolume --sys-prefix

# Notebooks w/ extensions that auto-run code must be "trusted" to work the first time
jupyter trust CPMG_NMR_relax_interactive.ipynb
jupyter trust CPMG_tcp.ipynb
jupyter trust plot_sin.ipynb

# Get relax
mkdir -p $HOME/software
cd $HOME/software
git clone https://github.com/nmr-relax/relax.git relax
cd $HOME/software/relax
git pull
scons
./relax -i
ln -s $HOME/software/relax/relax /srv/venv/bin/relax